# Requirements file for F1 Race Weekend Companion
#
# PREFERRED: Use uv for faster installs
#   uv pip install -e ".[dev,infra]"
#
# This file exists for compatibility with tools that require requirements.txt
# For full dependency management, see pyproject.toml

# Core dependencies
boto3>=1.34.0
botocore>=1.34.0
requests>=2.31.0
pydantic>=2.5.0
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.7.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
black>=23.12.0
ruff>=0.1.9
mypy>=1.8.0
boto3-stubs[bedrock-agent,bedrock-agent-runtime,dynamodb,lambda]>=1.34.0

# Infrastructure dependencies
aws-cdk-lib>=2.117.0
constructs>=10.3.0
